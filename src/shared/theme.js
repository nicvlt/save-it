const lightOrange = '#ffb576'
const darkOrange = '#fea55b'
const whiteContrast = 'hsl(27, 100%, 94%)'
const activable = 'rgba(59, 47, 60,1)'
const activableHovered = 'rgba(59, 47, 60, 0.9)'

export const theme = {
    lightOrange,
    darkOrange,
    whiteContrast,
    activable,
    activableHovered,
}